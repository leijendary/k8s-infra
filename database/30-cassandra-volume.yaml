apiVersion: v1
kind: PersistentVolume
metadata:
  name: cassandra-vol
  labels:
    type: local
spec:
  storageClassName: ""
  claimRef:
    name: cassandra-pvc
    namespace: database
  volumeMode: Filesystem
  capacity:
    storage: 1G
  accessModes:
    - ReadWriteOnce
  local:
    path: /usr/local/k8s/cassandra
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - docker-desktop
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cassandra-pvc
  namespace: database
spec:
  storageClassName: ""
  volumeName: cassandra-vol
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1G
